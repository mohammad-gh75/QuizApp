<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.maktab36.quizapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.SettingActivity"
            android:label="@string/setting_label"/>
        <activity android:name=".controller.QuizBuilderActivity"
            android:label="@string/quiz_builder_label">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.CheatActivity"
            android:label="@string/cheat_label"/>
        <activity android:name=".controller.QuizActivity" />
    </application>

</manifest>